#include "pch.h"
#include "PoisonRoach.h"

PoisonRoach::PoisonRoach(sf::Texture texture, sf::Vector2f position, sf::Vector2f scale, sf::Vector2f speed, int cHealth) :
	Enemy(texture, position, scale, speed, cHealth)
{
	 
}
